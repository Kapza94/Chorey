# Drop your real app screenshots here

Capture these 5 screens **on your phone** (the TestFlight build, signed into your
real family so the data looks real), then save them here with these exact names:

| File | Screen to capture |
|------|-------------------|
| `shot-1.png` | **Kid home** — balance, the Spend/Save/Give buckets, chore list |
| `shot-2.png` | **The split** — the screen that shows a reward splitting 40/40/20 |
| `shot-3.png` | **Parent approvals** — the Chores board with "Needs your approval" |
| `shot-4.png` | **Levels / journey** — the level road / points / streak |
| `shot-5.png` | (optional) any extra hero screen, e.g. wishlist or giving |

## How to capture on iPhone
Open the app → go to each screen → press **Side button + Volume Up** → the
screenshot saves to Photos. AirDrop/email them to this Mac and rename per above.

## Then
Tell me they're in, and I'll un-comment the `<img>` slot in `screenshots.html`
for each panel and re-export the final exact-size PNGs ready for App Store Connect.
(The headline speech-bubbles, stickers and framing stay; only the phone content
becomes your real screen.)
